# Known Unsupported Features That May Give a Problem

##### OBJ Spec:
    - The OBJ spec refers to negative indices to start counting the vertice data from the element's position in the file to the top. 

